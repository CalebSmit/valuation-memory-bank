# Cloudflare Pages configuration
name = "valuation-memory-bank"
compatibility_date = "2024-01-01"

# Required: tells Cloudflare Pages where the built output lives
pages_build_output_dir = "dist/public"

# D1 database binding (for future use — SQLite-compatible)
# To enable: create a D1 DB and fill in database_id below
# [[d1_databases]]
# binding = "DB"
# database_name = "valuation-memory-bank"
# database_id = "YOUR_D1_DATABASE_ID_HERE"
